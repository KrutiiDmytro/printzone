stages:
  - deploy

deploy:
  stage: deploy
  script:
    - rsync -av --exclude='.git' $CI_PROJECT_DIR/ /var/www/task-23/
    - cd /var/www/task-23
    - docker compose up -d
    - docker compose exec -T php composer install --no-scripts
    - until docker compose exec -T database-replica pg_isready -U app -q; do echo "Waiting for replica..."; sleep 3; done
    - docker compose exec -T php php bin/console doctrine:migrations:migrate --no-interaction
    - docker compose exec -T php rm -rf var/cache/*
  only:
    - develop
