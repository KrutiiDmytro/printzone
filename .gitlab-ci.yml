stages:
  - validate
  - build
  - test
  - deploy

default:
  tags:
    - Веб-разработка

variables:
  DEPLOY_DIR: /var/www/app
  DEPLOY_USER: deploy
  PHP_IMAGE: php:8.2-cli-alpine
  COMPOSE: "docker compose -f compose.yaml -f compose.prod.yaml"
  GIT_CLEAN_FLAGS: -ffd
  LOCAL_IMAGE: task-25/php:latest

# ─────────────────────────────────────────────────────────────
# VALIDATE
# Швидкі статичні перевірки — один docker run з PHP_IMAGE
# ─────────────────────────────────────────────────────────────

lint:php:
  stage: validate
  script:
    - echo "=== PHP syntax check ==="
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        $PHP_IMAGE \
        sh -c "find src tests -name '*.php' -print0 | xargs -0 -P4 php -l"
    - echo "=== Validating composer.json ==="
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        composer:2 \
        composer validate --strict --no-check-publish
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'

audit:composer:
  stage: validate
  script:
    - echo "=== Composer security audit ==="
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        composer:2 \
        composer audit --format=plain
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'

# ─────────────────────────────────────────────────────────────
# BUILD
# Збираємо локальний образ паралельно з validate (needs: [])
# ─────────────────────────────────────────────────────────────

build:image:
  stage: build
  script:
    - echo "=== Building PHP image locally ==="
    - docker build
      -f Dockerfile.php
      -t task-25/php:$CI_COMMIT_SHA
      -t task-25/php:latest
      --cache-from task-25/php:latest
      .
    - echo "Image built task-25/php:$CI_COMMIT_SHA"
  needs: []
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'

# ─────────────────────────────────────────────────────────────
# TEST
# Використовує локальний образ — жодного pull з Docker Hub
# ─────────────────────────────────────────────────────────────

.test_base:
  stage: test
  before_script:
    - docker volume create composer-cache-task25 || true
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -v "composer-cache-task25:/composer-cache" \
        -w /app \
        -e COMPOSER_HOME=/composer-cache \
        $LOCAL_IMAGE \
        composer install --no-interaction --no-scripts --prefer-dist --optimize-autoloader
  needs:
    - build:image
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'

test:unit:
  extends: .test_base
  script:
    - echo "=== Unit Tests ==="
    - mkdir -p "$CI_PROJECT_DIR/reports"
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -v "composer-cache-task25:/composer-cache" \
        -w /app \
        -e APP_ENV=test \
        -e COMPOSER_HOME=/composer-cache \
        $LOCAL_IMAGE \
        php bin/phpunit --testsuite Unit --colors=never --log-junit /app/reports/junit-unit.xml
  artifacts:
    when: always
    reports:
      junit: reports/junit-unit.xml
    expire_in: 7 days

test:functional:
  extends: .test_base
  script:
    - echo "=== Functional Tests (SQLite) ==="
    - mkdir -p "$CI_PROJECT_DIR/reports"
    - |
      docker run --rm \
        -v "$CI_PROJECT_DIR:/app" \
        -v "composer-cache-task25:/composer-cache" \
        -w /app \
        -e APP_ENV=test \
        -e COMPOSER_HOME=/composer-cache \
        $LOCAL_IMAGE \
        php bin/phpunit --testsuite Functional --colors=never --log-junit /app/reports/junit-functional.xml
  artifacts:
    when: always
    reports:
      junit: reports/junit-functional.xml
    expire_in: 7 days

# ─────────────────────────────────────────────────────────────
# DEPLOY
# ─────────────────────────────────────────────────────────────

deploy:
  stage: deploy
  timeout: 40 minutes
  environment:
    name: production
    url: https://e-commerce.it.com
  script:
    - echo "=== Deploying to $DEPLOY_DIR ==="

    - rsync -a --delete
      --exclude='.git'
      --exclude='.env.local'
      --exclude='vendor'
      --exclude='node_modules'
      --exclude='var/'
      --exclude='tests/'
      "$CI_PROJECT_DIR/" "$DEPLOY_DIR/"

    - cd "$DEPLOY_DIR"

    - echo "--- Injecting secrets from CI variables into .env.local ---"
    - |
      upsert_env() {
        local key="$1" val="$2" file="$3"
        local tmp
        tmp=$(mktemp)
        if grep -q "^${key}=" "$file" 2>/dev/null; then
          grep -v "^${key}=" "$file" > "$tmp"
        else
          cp "$file" "$tmp" 2>/dev/null || touch "$tmp"
        fi
        printf '%s=%s\n' "$key" "$val" >> "$tmp"
        mv "$tmp" "$file"
      }
      upsert_env STRIPE_SECRET_KEY      "$STRIPE_SECRET_KEY"      "$DEPLOY_DIR/.env.local"
      upsert_env STRIPE_PUBLISHABLE_KEY "$STRIPE_PUBLISHABLE_KEY" "$DEPLOY_DIR/.env.local"
      upsert_env STRIPE_WEBHOOK_SECRET  "$STRIPE_WEBHOOK_SECRET"  "$DEPLOY_DIR/.env.local"

    - set -a && source .env.local && set +a

    - echo "--- Starting primary database ---"
    - $COMPOSE up -d database
    - |
      i=0
      until $COMPOSE exec -T database pg_isready -U app -q; do
        i=$((i+1))
        if [ $i -ge 30 ]; then
          echo "ERROR: Primary DB not ready after 60s"
          $COMPOSE logs database --tail=30
          exit 1
        fi
        echo "Waiting for DB ($i/30)..."
        sleep 2
      done
      echo "Primary DB is ready"

    - echo "--- Starting all services (using pre-built image from CI) ---"
    - $COMPOSE up -d --no-build
    - $COMPOSE exec -T nginx nginx -s reload

    - echo "--- Installing dependencies ---"
    - $COMPOSE exec -T php composer install
      --no-dev --no-interaction --no-scripts --prefer-dist --optimize-autoloader

    - |
      i=0
      until $COMPOSE exec -T database-replica pg_isready -U app -q; do
        i=$((i+1))
        if [ $i -ge 40 ]; then
          echo "WARNING: Replica not ready after 2min — continuing"
          $COMPOSE logs database-replica --tail=20
          break
        fi
        echo "Waiting for replica ($i/40)..."
        sleep 3
      done

    - echo "--- Running migrations ---"
    - $COMPOSE exec -T php php bin/console doctrine:migrations:migrate --no-interaction

    - echo "--- Clearing and warming up cache ---"
    - |
      timeout 120 $COMPOSE exec -T php php bin/console cache:clear \
        || echo "WARNING: cache:clear timed out, continuing"
      timeout 180 $COMPOSE exec -T php php bin/console cache:warmup --env=prod \
        || echo "WARNING: cache:warmup timed out, continuing"
      $COMPOSE exec -T php chown -R www-data:www-data var/cache var/log || true
      $COMPOSE exec -T php chmod -R 755 var/cache var/log || true

    - echo "--- Health check ---"
    - |
      i=0
      until $COMPOSE exec -T php php bin/console about > /dev/null 2>&1; do
        i=$((i+1))
        if [ $i -ge 15 ]; then
          echo "ERROR: App health check failed after 30s"
          $COMPOSE logs php --tail=20
          exit 1
        fi
        echo "Health check attempt $i/15..."
        sleep 2
      done
      echo "Application is healthy"
    - echo "=== Deploy complete ==="

  needs:
    - test:unit
    - test:functional
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'
