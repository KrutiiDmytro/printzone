framework:
    secret: '%env(APP_SECRET)%'
    http_method_override: false
    handle_all_throwables: true
    # Stateless API service — no session.
    session: false
    php_errors:
        log: true

when@test:
    framework:
        test: true
