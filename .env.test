# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# OAuth (контролери читають $_ENV у конструкторі — потрібні для PHPUnit)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost/auth/google/callback
GITHUB_CLIENT_ID=test-github-client-id
GITHUB_CLIENT_SECRET=test-github-client-secret
GITHUB_REDIRECT_URI=http://localhost/auth/github/callback

# Database for tests - SQLite (первинна та репліка вказують на один файл)
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test.db"
DATABASE_REPLICA_URL="sqlite:///%kernel.project_dir%/var/test.db"

# Або якщо потрібна PostgreSQL тестова база:
# DATABASE_URL="postgresql://user:password@127.0.0.1:5432/app_test?serverVersion=16&charset=utf8"

STORAGE_TYPE=local

# AWS — fake credentials to prevent SDK from polling IMDS endpoint (169.254.169.254)
# which causes ~4.5s delay per S3Client instantiation in Docker environments
AWS_ACCESS_KEY_ID=test-key-id
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_DEFAULT_REGION=eu-north-1
AWS_S3_BUCKET=test-bucket
AWS_S3_VERSION=latest

# Stripe — dummy key for tests (real calls never happen in unit/functional tests)
STRIPE_SECRET_KEY=sk_test_dummy
STRIPE_PUBLISHABLE_KEY=pk_test_dummy
STRIPE_WEBHOOK_SECRET=whsec_dummy